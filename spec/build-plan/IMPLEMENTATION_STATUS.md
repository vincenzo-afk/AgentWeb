# Implementation Status

This document maps the runnable repository to the implementation-facing specification. It is intentionally evidence-based: a module is marked implemented only when its interface, behavior, and tests exist in the source tree.

## Current build slice

| Module | Status | Evidence | Next completion work |
|---|---|---|---|
| API tier | Implemented | `src/agentweb/api.py` exposes health, solve with synthesis output formats, observe/list, search, crawl/history, extract, memory, report, authenticated admin key/audit/usage/data-deletion/metrics/browser-credential/session-state routes, organization-scoped ownership checks, request IDs, scope auth, restricted CORS, per-identity rate limiting with response headers, cursor pagination, idempotent mutating operations, canonical `/v1/...` path routing, additive success response metadata, and solve-level execution transparency. Successful JSON object responses retain endpoint-specific fields and include reserved `_meta` metadata plus `X-AgentWeb-API-Version: v1`; unversioned paths remain a compatibility bridge marked `Deprecation: true`, with canonical `/v1` paths in metadata. Idempotent replays preserve business fields while receiving request-scoped metadata; 204 responses remain bodyless and errors retain their stable shape. Unsupported future major prefixes are not routed to v1 handlers. Startup resolves platform secrets through the provider boundary. | Deferred plan/execute/graph endpoints remain out of scope. |
| Search | Implemented | `src/agentweb/search.py` defines a pluggable provider protocol, free DuckDuckGo HTML adapter, configurable HTTP JSON provider, freshness forwarding, normalized result limits, typed provider failures, fallback behavior, bearer authentication, and published-date alias normalization. | Add licensed-provider-specific adapters and richer freshness metadata when a deployment supplies them. |
| Parser | Implemented | `src/agentweb/parser.py` parses HTML, JSON, text, and PDF fallbacks with warnings, HTML tables, and deterministic entity candidates. | Add richer layout parsing when a dependency policy permits it. |
| Normalizer | Implemented | `src/agentweb/normalizer.py` canonicalizes prices and dates across documented locale formats, recognizes ISO currency codes/symbols and grouped decimal conventions, preserves raw values on failure, and emits deterministic confidence values reflecting normalization success. | Expand locale coverage only with explicit fixtures for additional currencies/calendars; avoid guessing ambiguous formats. |
| Crawler | Implemented | `src/agentweb/crawler.py` performs bounded same-origin breadth-first traversal with URL deduplication, depth/page limits, robots handling, truncation reporting, thread-safe host-scoped minimum request intervals, redacted page failures, tenant-scoped durable crawl runs/page metadata, immutable page snapshots with bounded parser projections, and optional PostgreSQL-coordinated organization/host token buckets. The API exposes idempotent crawl creation plus tenant-scoped run listing/detail retrieval. | Add richer crawl scheduling and calibrated evaluation only if a later scope explicitly requires them. |
| Extractor | Implemented | `AgentWebEngine.extract` returns parsed metadata, links, tables, entities, warnings, trust score, overall confidence, explainable confidence reasons, field-level confidence, source-local evidence spans, and schema-guided normalized fields with deterministic normalization confidence. | Expand document-layout and locale-specific extraction under an approved dependency policy. |
| Memory | Implemented | `src/agentweb/memory.py` stores immutable organization-scoped content versions with latest lookup, hash diff, tenant-aware freshness reuse windows, monitor state, bounded structured parser projections for monitor comparison, tenant-owned scheduler jobs, leases, retries, dead-letter state, 24-hour idempotency records, monthly usage aggregates, and durable crawl history. `retention_gc` jobs carry bounded policy windows, purge expired snapshots/crawls locally with organization filters, and use the optional PostgreSQL queue for distributed lease ownership. Expired records and browser state have explicit scoped deletion paths. Production relational ownership is exposed separately through `rdbms.py`; runtime cutover remains explicit. | Relational runtime parity and production migration remain explicit deployment work. |
| Trust Engine | Implemented | `src/agentweb/trust_engine.py` blocks unsafe target classes by default, rejects URL credentials and malformed ports, rechecks redirect destinations, and supports explicit blocked domains. | Continue policy-aware decisions at the crawler boundary. |
| Ranking | Implemented | `src/agentweb/ranking.py` combines trust, task relevance, corroboration, publication recency, content type, and extraction confidence into deterministic ordering. | Add calibrated evaluation against a labeled relevance set. |
| Synthesis | Implemented | `src/agentweb/synthesis.py` provides deterministic text, comparison, timeline, and JSON rendering; every supported response includes line-level source-backed claim spans, an evidence score, explicit conflicts, and `insufficient_evidence` handling. Direct URL extraction now passes bounded tables/entities into field-level rendering and source-specific citation spans. | Add calibrated evaluation against broader structured datasets. |
| Monitor | Implemented | Monitor creation, cursor-paginated listing, request-driven and scheduled checks, webhook URL persistence, usage accounting, explicit `check_failed`/`no_change`/`change_detected` events, task-aware full-content/price/availability policies, persisted absolute/relative price thresholds, availability target states, whitespace-insensitive comparison, polling-visible webhook delivery state, and deterministic `structured_field` policies over bounded parsed projection paths are implemented. Structured fields support string/entity/price/date normalization, optional price thresholds, persisted structured snapshot projections, irrelevant-content suppression, and missing-field transitions. | Add calibrated evaluation against broader structured datasets and provider-specific parsing only under an approved dependency policy. |
| Scheduler | Implemented | `src/agentweb/scheduler.py` executes tenant-scoped `monitor_check`, `webhook_delivery`, and `retention_gc` jobs with lease-token ownership, frequency priorities, retries, organization rate limits, at-least-once success checks, and dead-letter transitions; `agentweb --worker` runs it as a separate process. `agentweb gc --schedule` enqueues bounded retention cleanup, and `PostgresDistributedQueue` uses row-locked token buckets and `FOR UPDATE SKIP LOCKED` claims when explicitly enabled. | Add production load testing and operational alerting for multi-node deployments. |
| Alerting | Implemented | `src/agentweb/alerting.py` signs payloads with HMAC-SHA256 and performs one bounded outbound attempt per queue lease; SQLite persists delivery attempts, statuses, retry state, and dead-letter outcomes without storing signing secrets. Distributed mode consumes the organization webhook limiter from PostgreSQL. | Add provider-specific delivery observability and production load testing. |
| Observability | Partial | `src/agentweb/trace.py` persists redacted organization-scoped execution spans, including browser operations, and the API exposes tenant-filtered `/report/{execution_id}` plus read-only `/report/{execution_id}/replay` ordered node/edge projections with explicit no-network/no-side-effect flags; `KeyStore` persists immutable admin audit events with exact action/actor/target and UTC time-range filters; `/admin/usage` returns organization-scoped monthly usage summaries; `/admin/metrics` returns organization-filtered counters, observations, and gauges backed by durable SQLite `MetricStore` points; monitor polling exposes redacted webhook delivery state; `agentweb gc` cleans snapshots, traces, metrics, and 730-day-default audit events; explicit PostgreSQL distributed mode now aggregates metric points through the shared coordinator; unauthenticated `/health` now checks memory, metrics, audit storage, and the optional queue coordinator and returns a non-sensitive degraded 503 when an enabled dependency fails. | Add managed/operator integration only if separately approved; no hosted monitoring service is implied by the local health contract. |
| Graph | Deferred | Explicitly post-MVP in the roadmap. | Do not implement until Phase 2 scope is approved. |
| Browser | Implemented | `src/agentweb/browser.py` provides optional Playwright/Chromium sessions with fresh contexts, same-origin egress filtering, bounded actions, retries, partial results, credential-bearing URL rejection, secret-safe `fill_credential` actions, redacted outputs, organization-tagged traces, and a bounded spawned process pool with worker recycling, explicit shutdown, and typed timeout/unavailable errors. `src/agentweb/credentials.py` and `src/agentweb/browser_sessions.py` store tenant-scoped credentials and origin-bound Playwright storage states encrypted with provider-backed Fernet keys and expose metadata-only lifecycle endpoints. `session_state_id` reuse is organization/origin checked; cookies, browser storage, and session tokens are never returned or written to traces. The planner now escalates explicit render/interaction tasks with absolute URLs through the isolated browser path, passes only bounded actions and opaque references, and fails closed on invalid authorized-state references. | CAPTCHA/MFA automation remains a non-goal; add provider-specific session bootstrap only under an approved dependency policy. |
| Planner/Router/Skills | Implemented | `src/agentweb/planner.py` defines serializable `Plan`/`PlanStep` contracts, deterministic intent classification, mode estimation, direct-URL handling, conservative unmatched-task fallback, and explicit skill validation. `src/agentweb/skills.py` provides thread-safe generic skill registration/matching with built-in comparison, price/availability, and source-summary templates; task content is not persisted. `src/agentweb/router.py` maps plans to bounded `ToolCall` objects, expands URL extraction steps, and supports documented reusable step aliases. `AgentWebEngine.solve` now uses the planner/router boundary and returns sanitized `plan`, `selection_logic`, and `actions` summaries while preserving grounded retrieval, tenant-scoped memory, redacted traces, and existing response fields. | Public `/plan` and `/execute` endpoints, connector-specific routing, and learning-loop persistence remain deferred. |

## Build order for this iteration

This slice adds historical replay on top of the persisted execution trace: **tenant-authorized trace → sanitized ordered nodes and edges → explicit historical/no-side-effect projection**. Public `/plan` and `/execute` agent APIs, graph reasoning, intervention, connector-specific routing, learning-loop persistence, CAPTCHA/MFA automation, and event-driven workflow automation remain out of scope because the project scope explicitly defers them.

## Done criteria for this slice

A module is considered complete only when its documented interface is implemented, failure behavior is tested with local fixtures, invariants are preserved, public behavior is documented, and the code does not persist secrets or treat an unreachable monitor as a successful no-change check. The current API slice additionally requires organization-scoped idempotency conflicts/replays, bounded cursors, and redacted usage output.

## Deliberate local-MVP constraints

The API reliability slice uses SQLite for deterministic local persistence. Idempotency records expire after 24 hours, usage is a local estimated-cost summary rather than an external invoice, and pagination is bounded to 100 records per request. The optional distributed queue mode uses PostgreSQL row locks for multi-instance job ownership and rate-limit coordination, but does not imply full business-record runtime cutover or distributed API idempotency.

The default implementation remains standard-library only. Rendered browser support is an optional free Playwright extra using an environment-provided Chromium binary. Platform secrets use a fail-closed provider boundary in staging/production; customer API keys remain PBKDF2-derived hashes, never plaintext. The optional PostgreSQL adapter provides the relational schema, required tenant indexes, bounded pooling, and additive migration import/export; the local runtime keeps SQLite snapshots/monitor state until an explicit production cutover. Snapshots, monitors, jobs, traces, keys, and audit events are organization-scoped. Scheduler execution is a separately supervised foreground worker backed by tenant-owned SQLite leases, retries, and dead-letter state by default; `AGENTWEB_DISTRIBUTED_QUEUE=1` switches queue claims and scheduled/webhook limiter state to PostgreSQL. Webhooks are opt-in and are not sent unless a monitor explicitly supplies a URL and a signing secret is configured.
